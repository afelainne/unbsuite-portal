import ToolLayout from "@/components/ToolLayout";
import UnbsFontApp from "@/tools/unbsfont";

const UnbsFont = () => (
  <ToolLayout title="UNBSFONT" chrome="app">
    <UnbsFontApp />
  </ToolLayout>
);

export default UnbsFont;
