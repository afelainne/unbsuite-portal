import ToolLayout from "@/components/ToolLayout";

const UnbsGrid = () => {
  return (
    <ToolLayout title="UNBSGRID">
      <div className="flex min-h-[60vh] items-center justify-center">
        <div className="text-center space-y-3">
          <h2 className="text-2xl font-black tracking-tight uppercase">UNBSGRID</h2>
          <p className="text-muted-foreground">Ferramenta de grid — pronta para integração</p>
        </div>
      </div>
    </ToolLayout>
  );
};

export default UnbsGrid;
