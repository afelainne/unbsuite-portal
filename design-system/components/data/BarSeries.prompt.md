Dense hairline bar chart. The house data texture.

```jsx
<BarSeries height={140} data={values} tone="gray" highlightIndex={46} />
```

Keep bars thin (1-3px) and numerous; let the eye read the envelope, not individual bars. Pair with an InsightPopover for the one value that matters.
