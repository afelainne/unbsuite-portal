The AI composer. Exactly one per screen, pinned bottom-centre and floating over content.

```jsx
<CommandBar suggestions={['Which exports failed?','Summarise this week']} onSubmit={ask} />
```

The mint square on the left is the only mint-filled control on the screen — it is the product's AI signature. Placeholder text is centred, sentence case, no period.
