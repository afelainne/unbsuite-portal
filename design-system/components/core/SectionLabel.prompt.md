The brand's section marker: a 9px dot followed by a tracked uppercase label.

```jsx
<SectionLabel>Quick actions</SectionLabel>
```

Mint dot by default; `tone="ink"` when the section is neutral or structural (e.g. "UI structure"). Write the label in sentence case — the component uppercases it.
