Small pill for a delta, count or flag.

```jsx
<Badge tone="accent">+32%</Badge>
```

Tones: `accent` (mint — positive movement), `invert` (black — a chart callout or hard fact), `neutral`, `outline`, `quiet` (mint-050 wash with mint-600 ink).
