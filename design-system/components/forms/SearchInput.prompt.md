Single-line text field. Search is the canonical use.

```jsx
<SearchInput detached placeholder="Type a tool name or ID..." />
```

`detached` splits the magnifier into its own square button (workspace header pattern). `tone="dark"` for fields inside the black command bar.
