Binary setting toggle.

```jsx
<Switch checked={auto} onChange={setAuto} label="Auto-export on publish" />
```

On is mint track + black knob. Intentional addition (see readme "Intentional additions") — the source kit has no switch, but settings surfaces need one.
