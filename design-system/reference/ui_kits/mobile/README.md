# UI kit — UNBSTOOLS mobile app

390 × 844 (iPhone 15 Pro). Open `index.html`; the bottom tab bar is live.

| Screen | Notes |
| --- | --- |
| Overview | Same information architecture as the desktop front page, stacked: statement label, series toggle, centred period tabs, 44px hero metric, hairline chart with a floating insight, then metric cards. |
| Library | Tool rows with 46px inset thumbnails and status badges. |
| Settings | Switch group and plan card. |

Mobile keeps the 52px tap target for every tab-bar glyph. The AI entry point moves from the floating command bar into the mint icon button in the header.

**Deliberately blank:** the Automations tab shows an explicit "not part of this kit" state.
